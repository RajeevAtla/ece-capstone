train: ./dataset/images  # Training data path
val: ./dataset/images  # No validation, but YOLO requires a path
nc: 1  # Number of classes (adjust if needed)
names: ["car"]  # Modify as per your dataset